version: '3.8'

services:
  api_estoque:
    # O 'build' aponta para a pasta onde está o Dockerfile
    build: ./app
    container_name: stock-prediction-api
    ports:
      - "8000:8000"
    restart: always
    # Ajuste para garantir performance no seu chip M4
    platform: linux/arm64